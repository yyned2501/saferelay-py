Read CLAUDE.md for the full spec. Then read the original worker.js from https://raw.githubusercontent.com/qianqi32/SafeRelay/main/worker.js in chunks (curl -s) until you fully understand every feature.

Now implement the COMPLETE project in /home/hermes/projects/saferelay-py/

Architecture requirements:

1. CORE LAYER — all third-party libs go through core/ only:
- core/__init__.py
- core/logger.py: wraps logging, with sanitize_sensitive()
- core/database.py: wraps aiosqlite, singleton Database class with ALL methods listed in CLAUDE.md
- core/http.py: wraps httpx.AsyncClient, get/post/close methods
- core/bot.py: wraps pyrogram.Client. Exports Bot class AND re-exports Message, CallbackQuery, InlineKeyboardMarkup, InlineKeyboardButton, filters, Chat, User, ForumTopic from pyrogram

2. SERVICES — business logic, only import from core/ (never pyrogram/aiosqlite/httpx):
- services/__init__.py
- services/verify.py: local quiz with InlineKeyboard (15 questions from original JS), pending queue, verification locks
- services/forward.py: bidirectional forwarding, topic mode, edit sync, media groups
- services/security.py: rate limiter, whitelist/blacklist, fraud db check, union ban API
- services/stats.py: daily counts, active users, broadcast support

3. HANDLERS — TG event routing, only import from core/ and services/:
- handlers/__init__.py
- handlers/user.py: guest messages, verification trigger, /start
- handlers/admin.py: /menu, /ban, /unban, /trust, /untrust, /welcome, /autoreply, /broadcast, /cleanup, /cachestats, /clearcache
- handlers/callback.py: quiz answers, menu navigation, verify mode toggle

4. config.py: reads from env BOT_TOKEN/ADMIN_IDS/GROUP_ID/WELCOME_MSG/AUTOREPLY_MSG, validates on import

5. main.py: wires db init, creates services with db+bot, registers handlers, bot.run()

ABSOLUTE RULES:
- handlers/ and services/ must NEVER import from pyrogram, aiosqlite, or httpx directly. Only from core/
- Use uv pip install if packages missing
- All files need type hints and Chinese docstrings
- Remove Turnstile completely — only local quiz verification
- Implement topic/forum mode with create_forum_topic on first message
- Store pending messages in database when user is not verified
- Handle edited messages sync
- Support media group (album) forwarding

After ALL files are written, run: bash /home/hermes/projects/saferelay-py/verify_imports.sh

And fix any import errors until it passes.

Do NOT stop until all files pass the import check.
