# SafeRelay-Py 项目重构

## 当前问题

当前项目 `/home/hermes/projects/saferelay-py/` 需要按以下规范重构：

### 1. 超限文件拆分（单文件 ≤ 300 行）

- `core/database.py`（619行）→ 拆出 `core/models.py`（ORM 模型定义），database.py 只保留 Database 类
- `services/forward.py`（412行）→ 拆出 `services/forward_topic.py`（话题模式逻辑）、`services/forward_edit.py`（编辑同步逻辑）
- `handlers/callback.py`（309行）→ 把回调处理中较重的逻辑移到 services 层

拆分后保证 import 链不走样：旧功能完全保留，不删代码，只重新组织。

### 2. 补齐文档

- 创建 `.clinerules`：包含所有 10 条偏好规则（见下方的规范模板）
- 创建 `ARCHITECTURE.md`：文件树、模块职责、引用拓扑

### 3. 规范模板

**`.clinerules` 内容要点**：
- Core 封装层：handlers/services 不能直接 import 第三方库
- 数据库：SQLAlchemy async ORM，不写 raw SQL
- 全栈 async，入口用 await start + idle + stop
- 单文件 ≤300 行
- 类型注解 + 中文 docstring
- logger 替代 print
- Git commit 中文
- 配置统一读取
- 每个包有 __init__.py，绝对导入
- 修改前先读 ARCHITECTURE.md

### 执行步骤

1. 先读所有要拆分的文件理解代码
2. 拆分 core/database.py → core/models.py + core/database.py
3. 拆分 services/forward.py → services/forward.py + services/forward_topic.py + services/forward_edit.py
4. 精简 handlers/callback.py
5. 更新所有 affected imports
6. 创建 .clinerules 和 ARCHITECTURE.md
7. 运行 `bash verify_imports.sh` 验证导入链
8. 修到通过为止

**不要停，直到所有问题修复**。
